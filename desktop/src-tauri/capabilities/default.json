{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the note window is allowed to ask the Rust side for. Deliberately short: everything the window does by itself — the hotkey, the tray, restoring position — is decided in Rust, where the frontend cannot reach it. Adding a permission here widens what a bug in the webview can do, so each one is here because a specific piece of the interface needs it.",
  "windows": ["note"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-start-dragging",
    "core:window:allow-set-focus",
    "core:window:allow-is-visible",
    "core:event:default",
    "autostart:allow-is-enabled",
    "autostart:allow-enable",
    "autostart:allow-disable"
  ]
}
